pipeline {
    agent any

    parameters {
        string(name: 'INSTANCE_ID', description: 'Instance ID to stop')
    }

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('AWS CLI Stop') {
            steps {
                script {
                    sh '''
                        echo "Stopping EC2 instance via AWS CLI..."
                        cd aws_cli_bash_scripts
                        bash -x ./stop_instance.sh "${INSTANCE_ID}"
                    '''
                }
            }
        }
    }

    post {
        always {
            cleanWs()
        }
    }
}
